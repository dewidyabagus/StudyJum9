package main

import (
	"log"
)

func main() {
	// Infinity for
	/*
		for {
			// blok kode
		}
	*/
	for {
		log.Println("Perulangan tak terhingga seperti kasih ibu")
	}

	// Membuat ticker contoh untuk mengambil data setiap 5 seconds
	/*
		Casting value:
			var i uint16 = 1
			var n uint   = 3
			var m = i + n    | Invalid code
			var m = uint(i) + n
	*/

	// ticker := time.NewTicker(5 * time.Second)
	// defer ticker.Stop()

	// for {
	// 	<-ticker.C
	// 	log.Println("Lakukan proses setiap 5 detik")
	// }
}
